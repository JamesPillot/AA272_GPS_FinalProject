function [meas, state] = simSystem(x0,xdot0, u, time_to_run, parmams)
% Simulates system
% Takes in initial conditions (x0 and xdot0), system inputs (u), the
% time to run (time_to_run), and the system parameters (params), and 
% returns a log of the state and the measured variables of the system

t = 0;
while(t < time_to_run)

end
end